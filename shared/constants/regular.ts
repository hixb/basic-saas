export const REGULAR = {
  // Email
  Email: /^[\w.%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}$/i,
  // Must contain at least one uppercase letter
  ContainUppercaseLetter: /[A-Z]/,
  // Must contain at least one number
  ContainLeastOneNumber: /\d/,
}
